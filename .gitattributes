# Normalise line endings so a checkout on Windows and one on Linux agree.
* text=auto

# Lockfiles are generated; a conflict in one is resolved by regenerating,
# not by merging line by line.
package-lock.json -diff linguist-generated

*.png binary
