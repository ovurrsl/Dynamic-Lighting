<Page
    x:Class="Dynamic_Lighting.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dynamic_Lighting.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Global Synchronization Dashboard" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Coordinate lighting patterns, custom palettes, and sync speeds globally across all active PC hardware." Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" FontSize="13"/>
        </StackPanel>

        <!-- Main Dashboard Content -->
        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 1: Sync Controls & Effect Cards -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="16" Margin="0,0,0,10">
                    
                    <!-- Sync Configuration Card -->
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="20">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Setting" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <TextBlock Text="Sync Settings" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <!-- Target Device Selection -->
                            <StackPanel Spacing="6">
                                <TextBlock Text="Target Device Selection" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                <ComboBox x:Name="TargetDeviceComboBox" HorizontalAlignment="Stretch" SelectedIndex="0" SelectionChanged="TargetDeviceComboBox_SelectionChanged">
                                    <ComboBoxItem Content="All Connected Devices (Global Sync)"/>
                                    <ComboBoxItem Content="Arduino Hardware (LampArray)"/>
                                    <ComboBoxItem Content="ASUS Aura System (Motherboard / ARGB)"/>
                                    <ComboBoxItem Content="Corsair iCUE Link Hub"/>
                                </ComboBox>
                            </StackPanel>

                            <!-- Lighting Mode Selector -->
                            <StackPanel Spacing="6">
                                <TextBlock Text="Active Lighting Effect" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                <ComboBox x:Name="ModeComboBox" HorizontalAlignment="Stretch" SelectedIndex="0" SelectionChanged="ModeComboBox_SelectionChanged">
                                    <ComboBoxItem Content="Static Solid Color (No animation)"/>
                                    <ComboBoxItem Content="Breathing (Smooth pulse effect)"/>
                                    <ComboBoxItem Content="Rainbow (Continuous hue spectrum cycle)"/>
                                    <ComboBoxItem Content="Audio-Reactive (Pulse to sound / Music)"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Advanced Parameters Card (Visible for Breathing/Rainbow) -->
                    <Border x:Name="ParametersCard"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="20"
                            Visibility="Collapsed">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="OutlineStar" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <TextBlock Text="Effect Parameters" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Speed / Frequency Slider -->
                            <StackPanel Spacing="6">
                                <Grid>
                                    <TextBlock Text="Animation Cycle Speed" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                    <TextBlock x:Name="SpeedTextValue" Text="1.0x" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                </Grid>
                                <Slider x:Name="SpeedSlider" Minimum="0.1" Maximum="5.0" StepFrequency="0.1" Value="1.0" ValueChanged="SpeedSlider_ValueChanged"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- System Status Card -->
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="16">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ProgressRing x:Name="StatusProgressRing" Grid.Column="0" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
                            <SymbolIcon x:Name="StatusIcon" Grid.Column="0" Symbol="Accept" Foreground="Green" VerticalAlignment="Center"/>
                            <TextBlock x:Name="StatusText" Grid.Column="1" Text="Ready to sync. Change color or effect." FontSize="13" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Column 2: Color Picker & Real-time Live Preview Glow -->
            <Border Grid.Column="1" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="24">
                <Grid RowSpacing="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="View" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                        <TextBlock Text="Visualizer &amp; Palette" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Real-time Synchronized Glow Visualizer -->
                    <Grid Grid.Row="1" Height="140" HorizontalAlignment="Stretch" Margin="0,10">
                        <!-- Outer Ambient Light Aura -->
                        <Ellipse x:Name="VisualizerOuterGlow" Width="130" Height="130" Fill="Red" Opacity="0.15" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <Ellipse x:Name="VisualizerMediumGlow" Width="100" Height="100" Fill="Red" Opacity="0.3" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        
                        <!-- Core Ring -->
                        <Ellipse x:Name="VisualizerCore" Width="70" Height="70" Fill="Red" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Ellipse.Stroke>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#FFFFFF" Offset="0.0"/>
                                    <GradientStop Color="#88FFFFFF" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Ellipse.Stroke>
                        </Ellipse>
                        
                        <!-- Core Indicator Label -->
                        <TextBlock Text="SYNCED GLOW" FontSize="9" FontWeight="Bold" Foreground="White" Opacity="0.9" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- Color Picker Section -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ColorPickerContainer" Spacing="10" HorizontalAlignment="Center">
                            <Grid x:Name="GlobalColorPickerHost" />
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Application Operations -->
                    <StackPanel Grid.Row="3" Spacing="12" Margin="0,12,0,0">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="ApplyColorButton" Grid.Column="0" Content="Apply Settings &amp; Save" HorizontalAlignment="Stretch" Click="ApplyColorButton_Click" Style="{StaticResource AccentButtonStyle}" Padding="10"/>
                            <ToggleButton x:Name="LivePreviewToggle" Grid.Column="1" Content="Live Sync" IsChecked="True" Click="LivePreviewToggle_Click" Padding="10"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
