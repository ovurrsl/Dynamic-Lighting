<Window
    x:Class="Dynamic_Lighting.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dynamic_Lighting"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Dynamic Lighting">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <!-- Main Application Navigation -->
        <NavigationView x:Name="NavView" SelectionChanged="NavView_SelectionChanged" IsBackButtonVisible="Collapsed" PaneDisplayMode="Left" Visibility="Collapsed">
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="Home" Content="Ana Sayfa (Sync)" Icon="Home" />
                <NavigationViewItem Tag="Arduino" Content="Arduino (LampArray)" />
                <NavigationViewItem Tag="Asus" Content="ASUS Aura (Motherboard)" />
                <NavigationViewItem Tag="Corsair" Content="Corsair iCUE Link" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!-- Loading Overlay during startup hardware connect -->
        <Grid x:Name="LoadingOverlay" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <ProgressRing x:Name="ConnectingProgressRing" IsActive="True" Width="60" Height="60" />
                <TextBlock Text="Donanımlara bağlanılıyor..." Style="{ThemeResource BodyTextBlockStyle}" HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
