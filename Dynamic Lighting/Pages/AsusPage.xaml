<Page
    x:Class="Dynamic_Lighting.Pages.AsusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dynamic_Lighting.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="40">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="Motherboard (ASUS Aura)" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <ComboBox x:Name="AuraZoneComboBox" Width="250" HorizontalAlignment="Center" SelectedIndex="0" SelectionChanged="AuraZoneComboBox_SelectionChanged">
                <ComboBoxItem Content="All Zones (Sync)"/>
                <ComboBoxItem Content="Mainboard LEDs Only"/>
                <ComboBoxItem Content="Addressable Header 1 Only"/>
                <ComboBoxItem Content="Addressable Header 2 Only"/>
            </ComboBox>
            
            <!-- Addressable Header LED Count Settings -->
            <StackPanel x:Name="AddressableLedCountPanel" Spacing="8" HorizontalAlignment="Center">
                <TextBlock Text="Addressable Header LED Counts:" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                    <TextBlock Text="Header 1:" VerticalAlignment="Center"/>
                    <NumberBox x:Name="AddrHeader1LedCount" Value="79" Minimum="0" Maximum="500" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" Width="120" ValueChanged="AddrHeaderLedCount_ValueChanged"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                    <TextBlock Text="Header 2:" VerticalAlignment="Center"/>
                    <NumberBox x:Name="AddrHeader2LedCount" Value="60" Minimum="0" Maximum="500" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" Width="120" ValueChanged="AddrHeaderLedCount_ValueChanged"/>
                </StackPanel>
            </StackPanel>

            <Grid x:Name="AuraColorPickerHost" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                <Button x:Name="ApplyAuraColorButton" Content="Apply Color" Click="ApplyAuraColorButton_Click" Style="{StaticResource AccentButtonStyle}"/>
                <ToggleButton x:Name="LivePreviewAuraToggle" Content="Live Preview" Click="LivePreviewAuraToggle_Click" />
            </StackPanel>
            <TextBlock x:Name="AuraStatusText" Text="Status: Waiting..." HorizontalAlignment="Center" Foreground="Gray" TextWrapping="Wrap" MaxWidth="300" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
